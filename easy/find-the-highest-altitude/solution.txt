class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int height = 0, res = 0;
        for(int i=0;i<gain.size(); i++){
            height = height + gain[i];
            res = max(res, height);
        }
        return res;
    }
};
